# 서버 포트 (기본값: 4000)
PORT=4000

# PostgreSQL 데이터베이스 설정 (로컬)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=coffee_app
DB_USER=postgres
DB_PASSWORD=your_password_here

# Render 배포 시 자동으로 제공됨
# DATABASE_URL=postgresql://...

# 허용할 프론트엔드 주소 (배포 시 Render 프론트엔드 URL 입력)
CORS_ORIGIN=http://localhost:5173
